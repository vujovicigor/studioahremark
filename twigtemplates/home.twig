{% extends "base.twig" %}
{% block content %}

{% set projects = fetch("Project") %}

<div class="home">
  
  <div class="row no-gutters owl-carousel owl-theme home--container">
    {% for item in projects %}
        <div class="col-12 p-0 home--container" ><img src="img/{{ fetch('Image', { parent_id: project.id })[0].Image }}" class="home--container-pic"></div>
    {% endfor %}
  </div>

  <div class="home--container2 d-flex padding-y-60 padding-x-main">
    <div class="home--container2-side d-flex">
        <div class="home--container2-side-left font-24 font-bold">Turning brands into beliefs.</div>
    </div>
    <div class="home--container2-side d-flex flex-column padding-x-right-col">
        <div class="home--container2-side-right1 font-24 font-regular margin-y-text">CREDO means "I believe" and that's what we strive to deliver to our client: BELIEF!</div>
        <div class="home--container2-side-right2 font-14 font-regular margin-y-text">As a strategic branding & design agency, we craft every brand's belief around the heart of its truth, discovered through research, shaped by strategy and manifested by visual design. These elements, working in sync, inspire belief and, thus, aimed to transform your brand.</div>
        <a class="home--container2-side-right3 font-14 font-bold">SEE MORE</a>
    </div>
  </div>

  <div class="home--container3 d-flex flex-wrap">
    {% for item in projects %}
    {% if loop.index < '5' %}
    <div class="home--container3-project" style="background-image: url('img/{{ fetch("Image", { parent_id: item.id })[0].Image }}'); background-size: cover; background-repeat: no-repeat; background-position: center;">
        <div class="home--container3-project-hover padding-y-40 padding-x-box d-flex justify-content-between">
            <div class="home--container3-project-hover-text font-14 font-regular">{{item.Title}}</div>
            <div class="home--container3-project-hover-text font-14 font-regular">{{item.Category}}</div>
        </div>
    </div>
    {% endif %}
    {% endfor %}
  </div>

</div>

{% endblock %}
